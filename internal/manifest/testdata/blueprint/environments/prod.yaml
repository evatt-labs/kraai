kind: persistent
protected: true
naming:
  prefix: ""
routes:
  api:
    - pattern: api.acme.com
      custom_domain: true
      certificate: CERT
resources:
  api:
    database:
      DB: { id: 0e1f...-uuid }
