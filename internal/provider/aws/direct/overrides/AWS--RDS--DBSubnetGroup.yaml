type: AWS::RDS::DBSubnetGroup
read:
  model: rds/service/2014-10-31/rds-2014-10-31.json
  operation: DescribeDBSubnetGroups
  identifier:
    DBSubnetGroupName: DBSubnetGroupName
  # DescribeDBSubnetGroups filters by a single name and answers with a list.
  response: DBSubnetGroups[]
properties:
  DBSubnetGroupArn: DBSubnetGroupArn
  DBSubnetGroupDescription: DBSubnetGroupDescription
  DBSubnetGroupName: DBSubnetGroupName
  SubnetIds:
    member: Subnets[].SubnetIdentifier
skip:
  # ListTagsForResource takes the resource ARN as ResourceName.
  # DBSubnetGroupArn is mapped above, but a further call may only be
  # addressed by the primary identifier (DBSubnetGroupName); nothing
  # threads a mapped response value, or {region}/{account}, into one.
  Tags: ListTagsForResource needs the ARN, which no further call can be addressed by
absentIds:
  - kraai-absent-probe-0123456789
