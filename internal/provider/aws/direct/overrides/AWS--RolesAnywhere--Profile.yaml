# Joined mechanically by join_main.go; delete this line once reviewed.
type: AWS::RolesAnywhere::Profile
read:
  model: rolesanywhere/service/2018-05-10/rolesanywhere-2018-05-10.json
  operation: GetProfile
  identifier:
    ProfileId: profileId
  response: profile
properties:
  AcceptRoleSessionName: acceptRoleSessionName
  AttributeMappings:
    member: attributeMappings
    properties:
      CertificateField: certificateField
      MappingRules:
        member: mappingRules
        properties:
          Specifier: specifier
  DurationSeconds: durationSeconds
  Enabled: enabled
  ManagedPolicyArns: managedPolicyArns
  Name: name
  ProfileArn: profileArn
  ProfileId: profileId
  RoleArns: roleArns
  SessionPolicy: sessionPolicy
skip:
  Tags: no member of the read's resource structure matches Tags by name and type; not mapped mechanically
